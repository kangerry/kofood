import 'dart:typed_data';

class PickedData {
  final String name;
  final Uint8List bytes;
  PickedData(this.name, this.bytes);
}

Future<List<PickedData>> pickImages(int remaining) async {
  return [];
}
